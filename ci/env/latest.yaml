name: test
channels:
  - conda-forge
dependencies:
  - python>=3.10
  # required
  - folium
  # test (Python)
  - pytest
  - pytest-cov
  - codecov
  # test (JS browser)
  - playwright
  - pip
  - pip:
      - pytest-playwright
