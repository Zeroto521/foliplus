name: test
channels:
  - conda-forge
dependencies:
  # required
  - folium
  # test (Python)
  - pytest
  - pytest-cov
  - codecov
  # test (JS browser)
  - playwright
  - pip
  - pip:
      - pytest-playwright
