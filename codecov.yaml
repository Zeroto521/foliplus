coverage:
  precision: 2
  round: down
  status:
    project:
      default:
        target: 90%
        threshold: 2%
    patch:
      default:
        target: 80%

comment:
  layout: "header, diff, files"
  behavior: default
