coverage:
  precision: 2
  round: down
  status:
    project:
      default:
        target: 65%
        threshold: 2%
    patch:
      default:
        target: 60%

comment:
  layout: "header, diff, files"
  behavior: default
