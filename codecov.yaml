coverage:
  precision: 2
  round: down
  status:
    project:
      default:
        target: 95%
        threshold: 1%
    patch:
      default:
        target: 80%

comment:
  layout: "header, diff, files"
  behavior: default
