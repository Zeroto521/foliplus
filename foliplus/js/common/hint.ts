// Hint duration constants for foliplus components.
// Pure constant (no state) — imported statically by components at build time.
// The runtime hint/toast system (runtime/hint.js) also imports this.

export const HINT_DURATION = { SHORT: 1200, MEDIUM: 2500, LONG: 4000, PERSIST: 0 };
